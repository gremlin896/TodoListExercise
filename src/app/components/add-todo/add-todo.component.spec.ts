// TODO: Add tests

// it('should emit from add with form control value on button press');

// it('should emit from add with form control value on enter in input field');

// it('should clear form control after emit');
